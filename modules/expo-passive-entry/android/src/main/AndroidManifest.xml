<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
  <!-- Android 12+ split the install-time BLUETOOTH/BLUETOOTH_ADMIN pair into runtime
       permissions. BLUETOOTH_SCAN is declared WITHOUT usesPermissionFlags="neverForLocation",
       deliberately (measured in the car 2026-09-04): with that flag the Bluetooth service drops
       every scan result whose record carries a location-class beacon — the iBeacon/Eddystone
       denylist in GattService — and the car's advertisement ALWAYS carries its iBeacon, so the
       controller matched our 1122 filter ~10×/s while the app received nothing at all. The price
       is ACCESS_FINE_LOCATION (which the app holds anyway) plus Location Services being on, which
       is exactly what the official Tesla app requires before it scans (BLEService.A0). -->
  <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
  <!-- The foreground service that replaces CoreBluetooth state restoration. connectedDevice is
       the type the official Tesla app declares (foregroundServiceType=0x10). -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

  <application>
    <service
        android:name="expo.modules.passiveentry.PassiveEntryService"
        android:exported="false"
        android:foregroundServiceType="connectedDevice" />
    <!-- Exported: BOOT_COMPLETED / MY_PACKAGE_REPLACED are protected system broadcasts, nothing
         else can send them. Internal actions go to the non-exported receiver below. -->
    <receiver
        android:name="expo.modules.passiveentry.PassiveEntryBootReceiver"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
      </intent-filter>
    </receiver>
    <receiver
        android:name="expo.modules.passiveentry.PassiveEntryReceiver"
        android:exported="false" />
  </application>
</manifest>
